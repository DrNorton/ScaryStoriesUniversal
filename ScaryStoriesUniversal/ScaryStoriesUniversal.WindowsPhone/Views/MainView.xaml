<Page
    x:Class="ScaryStoriesUniversal.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScaryStoriesUniversal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:micro="using:Caliburn.Micro"
  
    mc:Ignorable="d"

    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot" Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Hub  Header="Страшные истории">
           

            <HubSection Header="Меню">
                <DataTemplate>
                    <Button Content="Все истории"  
                            micro:Message.Attach="[Event Click] = [Action NavigateToAllStories()]">
                        
                    </Button>
                   
                </DataTemplate>
            </HubSection>

            <HubSection Header="Категории" FontSize="26.667">

            </HubSection>
        </Hub>
    </Grid>
</Page>
